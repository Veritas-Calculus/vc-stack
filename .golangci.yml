version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - gocritic
    - gocyclo
    - gosec
    - unconvert
    - bodyclose
    - gocognit
    - godot
    - nolintlint
    - whitespace
  settings:
    govet:
      enable:
        - shadow
    gocyclo:
      min-complexity: 30
    gocognit:
      min-complexity: 50
  exclusions:
    paths:
      - ".*rbd_manager\\.go$"
    rules:
      - path: _test\.go
        linters:
          - gocyclo
          - errcheck
          - gosec
          - gocognit
