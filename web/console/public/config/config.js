/* eslint-env browser */
// Runtime config injected at load time. Adjust to your environment.
// This file is served at /config/config.js by the backend and referenced in index.html.
// For production, override this file or set environment-specific values.
globalThis.__VC_CONFIG__ = {
  // Empty string means use relative paths (same origin).
  // Set to a full URL like 'http://10.31.0.3/api' to point to a remote API.
  apiBase: ''
}
